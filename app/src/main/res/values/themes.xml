<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.BeerCounter" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/gray</item>
    </style>

    <style name="Theme.BeerCounter" parent="Base.Theme.BeerCounter">
        <item name="colorPrimary">@color/brick</item>
    </style>

    <style name="Theme.BeerCounterModal" parent="Base.Theme.BeerCounter">
        <item name="colorPrimary">@color/dull_green</item>
    </style>
</resources>